/// Named mascot states (dev-prompt §5/§7) — any feature triggers one of
/// these on the shared MoMascot widget rather than writing new animation
/// code.
enum MascotState { idle, thinking, celebrating, alerting, remembering }
